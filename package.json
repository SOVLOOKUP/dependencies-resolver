{
  "name": "dependencies-resolver",
  "version": "0.1.2",
  "description": "detective your requires and install",
  "main": "dist/index",
  "scripts": {
    "build": "tsup index.ts --dts --minify --minify-whitespace --format esm,cjs --minify-identifiers --minify-syntax --clean"
  },
  "author": "sovlookup",
  "license": "MIT",
  "dependencies": {
    "detective": "^5.2.0"
  },
  "devDependencies": {
    "@nodelib/fs.stat": "^2.0.5",
    "@nodelib/fs.walk": "^1.2.7",
    "@types/detective": "^5.1.1",
    "@types/node": "^15.12.3",
    "tsup": "^4.11.2",
    "typescript": "^4.3.4"
  }
}
