{
  "name": "require-resolver",
  "version": "1.0.0",
  "description": "detective your requires and install",
  "main": "index.js",
  "scripts": {
    "build": "tsup index.ts --minify --minify-whitespace --format esm,cjs --minify-identifiers --minify-syntax --clean"
  },
  "author": "",
  "license": "ISC",
  "type": "module",
  "dependencies": {
    "detective": "^5.2.0"
  },
  "devDependencies": {
    "tsup": "^4.11.2"
  }
}
